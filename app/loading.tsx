import { PageSkeleton } from "@/components/ui/page-skeleton";

export default function Loading() {
  return <PageSkeleton title="Loading PlacementPrep AI" subtitle="Preparing the app shell." sections={2} />;
}

